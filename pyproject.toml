[project]
name = "shortcut-engine"
version = "0.1.0"
description = "A modular callback orchestration engine"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]